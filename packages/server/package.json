{
  "name": "@blog/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "@blog/schema": "workspace:*"
  },
  "peerDependencies": {
    "@hono/zod-validator": "^0.4.2",
    "drizzle-orm": "^0.38.2",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/bun": "^1.1.14",
    "vitest": "^2.1.8"
  }
}
